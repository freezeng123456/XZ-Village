import json
from pathlib import Path
root=Path.cwd();out=root/'outputs/orthogonal-multiview';v=json.loads((out/'validation.json').read_text());a=json.loads((out/'rebuild_validation.json').read_text());vr=json.loads((out/'visual_review.json').read_text());counts=v['counts'];changes=json.loads((out/'multiview_changes.json').read_text());ids=sorted(set(c['id'] for c in changes));pairs=json.loads((out/'plan_intersection_audit.json').read_text())['projected_plan_overlaps']
s=f'''# XZ Village · 去返程共同修订记录

日期：2026-09-13。交付文件：`Village_Multiview_Refined.blend`。本轮完成返程证据驱动的局部结构、分面外观与全体材质修订；这是包含估计的建筑重建，不是测绘验收结果。

原版本并非只用去程。297 个新增编号单元中，171 个以去程画面为主要参考，126 个以返程 256 秒为主要参考。之前的主要不足是单元各自依赖主要参考图，同一栋建筑的两侧没有充分交叉检查。

本轮重新查看去程 44 秒与返程 256 秒的对应区域，并用返程 252、260、264 秒相邻画面辅助判断遮挡、墙面方向及屋顶关系。17 个单元有明确的返程修正；其余 280 个单元沿用已有几何识别，更新统一材质与细部处理。没有宣称 297 个单元都完成逐栋双向摄影测量。

## 本轮实际修改

- B007、B008、B010、B012：允许直角凹入的六角平面，墙、楼板与屋顶使用相同的转角关系。相邻墙线保持垂直；缺角尺寸与向底层延伸仍是估计。其他建筑没有为了“矩形”而新加任意斜墙。
- B007：返程可见四排楼层，改为四层；保留屋顶高度，重新估计基底高度；增加成对背窗、较窄小窗、水箱和空调。
- B013：从错误的白色装修住宅改为三层砖混毛坯房，取消阳台与玻璃，保留真正的空窗洞、混凝土柱与楼板。
- B008、B009、B010、B012、B019：分别修正返程可见的墙色和窗洞密度，避免正面布局在背面机械重复。
- R001、R002、R007、R008、R009、R010、R013、R017、R021、R022：修正棕色或灰色墙面、红褐露台、蓝色平屋顶、浅蓝露台棚，以及由照片阴影造成的墙色误判。补齐 R007 可见的一排六扇窗、R013 的中央小窗，并恢复 R021 的大面积素墙。
- 297 个新增单元：收窄檐口与窗套，降低瓦片与铺砖的随机色差，使用较浅的灰色污迹；老屋装饰框采用较克制的灰褐色。玻璃保持实际几何和反射材质。
- 修掉试渲染中发现的缺角排水篦子悬空、屋顶边缘重叠黑线、楼板侧面共面黑缝与墙角重复端面。楼板边缘内收 1 厘米以消除共面表面；不会改变外墙轮廓。

重点修订编号：{', '.join(ids)}。完整参数及具体变更见 `profiles.json`、`inventory.json`、`multiview_changes.json`。

## 文件与几何检查

以下结论来自重新载入本版保存文件后的检查：

| 检查内容 | 结果 |
|---|---|
| 297 个编号与场景子集合一一对应 | 通过 |
| 实际保存的楼板与直角平面一致 | 297 / 297 通过；楼板边缘内收 1 厘米 |
| 墙板位于对应平面边界及高度范围 | 297 / 297 通过 |
| 非有限顶点、退化面、缺失材质 | 未检出 |
| 基准建筑保留几何 | 674 个对象，几何摘要与修订前相同 |
| 照片纹理节点、文件型图片、相机背景 | 均为 0 |
| 外部链接库、保存中的材质覆盖 | 均为 0 |
| 保存场景的建筑主体体积相交 | 未检出；不包含所有屋檐与附属小构件的精确碰撞检测 |
| 附带脚本完整重建并重新载入 | 几何摘要、对象数、顶点数与交付文件一致 |

新增建筑合计 {counts['new_objects']:,} 个对象、{counts['mesh_vertices']:,} 个网格顶点、{counts['openings']:,} 处建模开口。加上保留的基准建筑，共 298 个编号单元。编号包括侧翼与屋顶单元，不等同于独立宅基地或住宅户数。

重建一致性几何摘要：`{a['original']['architecture_geometry_sha256']}`。

## 视觉检查的实际范围

本轮交付包含去程、返程整村纯模型渲染，中性材质几何视图，现代住宅、瓦屋、太阳能和毛坯房细节图。19 页双面图册覆盖全部 297 个单元；已逐页检查整体轮廓、缺失构件、接缝与材质异常。17 个重点单元另外提供主要参考机位和正反斜视，共 51 张单体图；7 个去程单元额外提供返程机位图。

实际目视复核记录见 `visual_review.json`。图册检查不等同于所有窗洞和瓦片都与原视频逐像素核验；远景门窗、屋内结构与遮挡部分仍有推断。

## 仍有估计与未完成校准的内容

去程与返程没有完成统一的摄影测量束调整。返程镜头沿用手工屋顶控制点估计，因此对照图仍有位置和透视偏差；不得将原版本的投影误差统计当成本版双向精度验收。

四处缺角的精确尺寸、B007 的基底标高、不可见底层和背面细节都是建模假设。B014 的复杂蓝色坡顶、R002 后侧延伸、R004 的阶梯屋顶关系等仍保留简化表达，本轮没有把这些复杂连接声称为精确恢复。

检查发现 {len(pairs)} 对远景单元的水平投影相叠，但它们位于不同的估计高度区间。对应编号为 {', '.join(r['a']+' / '+r['b'] for r in pairs)}。现有主体未发生体积相交；这些投影重叠仍提示地形高差、屋顶归属和远景配准需要进一步校准，不能据此证明空间关系完全正确。屋檐及所有装饰小构件没有做逐三角形碰撞验收。

模型维持纯几何和程序材质。原视频画面只保存在独立 `reference/` 目录并在页面参考栏展示；未用照片铺地、立面覆图或相机背景来掩盖重建误差。
'''
(out/'RECONSTRUCTION_REPORT.md').write_text(s)
print('REPORT_READY')
